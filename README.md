# لوحة تحكم صالون التجميل (Laravel 9.2)

## 📋 نظرة عامة

**لوحة تحكم صالون التجميل** هو نظام متكامل لإدارة صالونات التجميل، مبني على Laravel 9.2 مع واجهة مستخدم حديثة باستخدام Hope UI Pro. النظام يدعم نموذج SaaS متعدد المستأجرين مع ميزات متقدمة لإدارة الحجوزات والخدمات والعملاء.

### ✨ المميزات الرئيسية

- 🏢 **نظام فرعي متكامل** لإدارة الصالونات
- 💇 **إدارة الخدمات** والمعالجات التجميلية
- 📅 **نظام الاشتراكات** والعضويات
- 🎨 **واجهة مستخدم حديثة** باستخدام Hope UI Pro
- 🔧 **بنية معيارية** (Modular Architecture)
- 🌐 **واجهة برمجة تطبيقات (API)** لجميع الوحدات
- ✅ **اختبارات آلية** لضمان الجودة
- 📱 **تصميم متجاوب** يعمل على جميع الأجهزة
- 🚀 **دعم مستقبلي** لـ React

## 🛠️ التقنيات المستخدمة

| التقنية | الإصدار | الوصف |
|---------|---------|-------|
| **Laravel** | 9.2 | إطار العمل PHP |
| **Vue.js** | 3.x | واجهة المستخدم التفاعلية |
| **Hope UI Pro** | Latest | قوالب واجهة المستخدم |
| **Bootstrap** | 5.x | إطار العمل CSS |
| **Bagisto** | Latest | نظام إدارة المخزون |
| **Font Awesome** | Latest | الأيقونات |
| **MySQL** | 8.0+ | قاعدة البيانات |

## 🚀 البدء السريع

### المتطلبات الأساسية

- PHP 8.0+
- Composer
- Node.js و NPM
- MySQL/MariaDB
- Git

### خطوات الإعداد

1. **استنساخ المستودع**
   ```bash
   git clone <repository-url>
   cd AdminPanelFrezka
   ```

2. **تثبيت حزم PHP**
   ```bash
   composer install
   ```

3. **تثبيت حزم Node.js**
   ```bash
   npm install
   ```

4. **إعداد ملف البيئة**
   ```bash
   cp .env.example .env
   php artisan key:generate
   ```

5. **تكوين إعدادات التطبيق في ملف .env**
   ```env
   APP_URL=http://127.0.0.1
   MIX_ASSET_URL=http://127.0.0.1
   
   DB_DATABASE=salon
   DB_USERNAME=اسم_المستخدم
   DB_PASSWORD=كلمة_المرور
   ```

6. **تشغيل الهجرات وبذر البيانات**
   ```bash
   php artisan migrate:fresh --seed
   ```

7. **بناء الأصول**
   ```bash
   npm run dev    # للتطوير
   npm run prod   # للإنتاج
   ```

8. **تشغيل خادم التطوير**
   ```bash
   php artisan serve
   ```

## 🏢 نظام SaaS متعدد المستأجرين

### المميزات

- 🔒 **إعدادات خاصة بكل شركة**
- 👥 **تسجيل دخول منفصل لكل شركة**
- 🛡️ **عزل كامل للبيانات بين الشركات**
- 🎯 **تخصيص لكل مستأجر**

### الإعدادات المتعددة المستأجرين

```php
// مثال للوصول إلى بيانات المستأجر الحالي
$tenant = tenant();
$settings = $tenant->settings;
$users = $tenant->users;
```

## 📁 هيكل المشروع

```
AdminPanelFrezka/
├── app/
│   ├── Http/
│   │   ├── Controllers/
│   │   └── Middleware/
│   ├── Models/
│   ├── Modules/
│   │   ├── Booking/
│   │   ├── Services/
│   │   ├── Inventory/
│   │   └── Customers/
│   └── Providers/
├── database/
│   ├── migrations/
│   └── seeders/
├── modules/
│   ├── Booking/
│   ├── Services/
│   ├── Inventory/
│   └── Customers/
├── resources/
│   ├── views/
│   ├── js/
│   │   └── components/
│   └── css/
├── routes/
│   ├── web.php
│   ├── api.php
│   └── tenant.php
├── tests/
├── .env.example
├── composer.json
├── package.json
└── README.md
```

## 🔧 الأوامر المتاحة

### أوامر التطوير

```bash
# تثبيت الاعتماديات
composer install
npm install

# بناء الأصول
npm run dev     # للتطوير
npm run prod    # للإنتاج
npm run watch   # للمراقبة

# قاعدة البيانات
php artisan migrate              # الهجرات فقط
php artisan db:seed             # بذر البيانات
php artisan migrate:fresh --seed # إعادة تعيين كامل

# تشغيل الخادم
php artisan serve
```

### أوامر الوحدات النمطية

```bash
# إنشاء وحدة جديدة
php artisan module:make ModuleName

# نشر وحدة
php artisan module:publish ModuleName

# تفعيل/تعطيل وحدة
php artisan module:enable ModuleName
php artisan module:disable ModuleName
```

### أوامر الاختبار

### المكونات المستخدمة

- **جداول البيانات (Datatables)** - للعرض والتصفية
- **Flatpicker** - لاختيار التواريخ
- **Vue.js** مع:
  - **Vee Validate** - للتحقق من صحة النماذج
  - **Pinia** - لإدارة الحالة
  - **Vue Store** - للتخزين

### مثال استخدام المتجر في Vue.js

```javascript
// استيراد المتجر
import { useCounterStore } from '../store/booking'
const store = useCounterStore()

// استخدام القيم المحسوبة
const doubleValue = computed(() => store.doubleCount)

// تحديث الحالة بشكل دوري
setInterval(() => {
  store.increment()
}, 500);
```

## 🌐 الترجمة والتدويل

### في ملفات PHP

```php
// استخدام الترجمة
__('save_form')
__('constant.title')
```

### في قوالب Blade

```blade
{{ __('constant.title') }}
@lang('are_you_sure?')
```

### في Vue.js

```javascript
// في المكونات
$t('constant.lbl_type')

// في الخصائص
:label="$t('constant.lbl_type')"
```

## 📊 مخططات التصميم

### مخططات النظام المتوفرة

- 📈 **مخطط تدفق البيانات (DFD)**
- 🗂️ **مخطط كيانات العلاقات (ERD)**
- 🎨 **تصاميم واجهات التطبيق**
- 📋 **دليل الأنماط والخطوط**

## 🔧 Git Hooks

### إعدادات Pre-commit

```json
{
  "husky": {
    "hooks": {
      "pre-commit": "./vendor/bin/pint",
      "pre-push": "php artisan test"
    }
  }
}
```

## 📚 الوثائق

### وثائق Laravel
- [https://laravel.com/docs/9.x](https://laravel.com/docs/9.x)

### وثائق Hope UI
- [https://pixinvent.com/demo/hope-ui-dashboard-laravel/documentation/](https://pixinvent.com/demo/hope-ui-dashboard-laravel/documentation/)

### وثائق الوحدات النمطية
- [https://docs.laravelmodules.com/v9/artisan-commands](https://docs.laravelmodules.com/v9/artisan-commands)

## 🚀 النشر

### متطلبات الإنتاج

- PHP 8.0+
- MySQL 8.0+
- Nginx/Apache
- Redis (اختياري)
- Supervisor (للqueues)

### خطوات النشر

1. **نسخ الملفات إلى الخادم**
2. **تثبيت الاعتماديات**
   ```bash
   composer install --no-dev --optimize-autoloader
   npm run prod
   ```
3. **إعداد البيئة**
   ```bash
   cp .env.example .env
   php artisan key:generate
   ```
4. **تشغيل الهجرات**
   ```bash
   php artisan migrate --force
   ```
5. **تحسين التخزين المؤقت**
   ```bash
   php artisan config:cache
   php artisan route:cache
   php artisan view:cache
   ```

## 🧪 الاختبار

### أنواع الاختبارات

- **Unit Tests** - اختبار الوحدات المنفردة
- **Feature Tests** - اختبار الوظائف المتكاملة
- **Browser Tests** - اختبار واجهة المستخدم
- **API Tests** - اختبار واجهات البرمجة

### تشغيل الاختبارات

```bash
# جميع الاختبارات
php artisan test

# اختبارات معينة
php artisan test --filter BookingTest

# تغطية الكود
php artisan test --coverage
#
